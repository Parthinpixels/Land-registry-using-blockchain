{
  "name": "land-registry-hardhat",
  "version": "1.0.0",
  "devDependencies": {
    "chai": "^4.3.7",
    "ethers": "^6.6.3",
    "hardhat": "^2.16.0"
  },
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy-local": "hardhat run scripts/deploy.js --network localhost"
  }
}
